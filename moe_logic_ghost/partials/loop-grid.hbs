{{#foreach posts}}
<article class="{{post_class}} post-stream-grid">
	<a href="{{url}}">
	<section class="grid-image">
		<img src="{{image}}" alt="">
	</section>
    <header class="post-stream_header">
        <h1 class="post-stream_header_title">{{{title}}}</h1>
    </header>
    </a>
</article>
{{/foreach}}

{{pagination}}